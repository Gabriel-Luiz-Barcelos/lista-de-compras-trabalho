import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import {Home} from './srv/screeans/home';

export default function App() {
  return (
   <Home>

   </Home>
  );
};
